RPC functions and helpers for Supabase

This folder contains SQL helper functions used by n8n workflows and server code.

upsert_customer_by_phone - upsert_customer_by_phone(p_clinic_id uuid, p_phone text, p_name text)
  Returns the customer row after insert/update.
