# 2026-05-09

- Continued work in `projects/dating-ai` on branch `feat/tinder-ai-task-3-ai-adapter` for the Tinder-first AI browser extension.
- Completed and verified the active-chat estimation slice for AutoPilot:
  - added lightweight `estimateActiveChats(...)`
  - wired estimate into the sniper path before `runSniper(...)`
  - fixed settings-key stability so optional fields do not leak literal `"undefined"`
- Added a lightweight integration-style AutoPilot handoff test by extracting async scheduling logic into `apps/extension/src/app/content/scheduler.ts`.
- Verified the extension after the new slices:
  - focused extension tests passed: `19/19`
  - root monorepo tests passed
  - focused content bundle check passed
  - full Chrome extension build passed
- Current notable commits on this work:
  - `88940cf` — `feat(extension): estimate active chats for autopilot`
  - `e126aee` — `test(extension): cover autopilot handoff flow`
- Build artifact confirmed at `apps/extension/build/dating-aiextension-1.2.2-chrome.zip`.
- Recommended next practical step: run the real Tinder manual smoke test using `docs/tinder-extension-smoke-test.md`, then prepare PR if clean.
- Live Tinder DOM research completed and documented in `projects/dating-ai/docs/tinder-dom-research-2026-05-09.md`.
- Important product note to keep in mind: the `Likes Sent` page (`/app/my-likes`) is a strong source for future features such as Expiry Radar, Super Like Optimizer, Sent-Like Triage, and outgoing-like analytics; the key selectors there include `button[aria-label^="Open "][aria-label$="'s profile"]`, `button[aria-label^="Super Like "]`, and `div[data-testid="likesYouCard"]`.


---

## Memory Flush — 2026-05-09 22:06 GMT+3

### dating-ai extension — Current Status (plan review)

**Branch:** `feat/tinder-ai-task-3-ai-adapter`
**Repo path:** `projects/dating-ai`
**Tests:** 19/19 passing
**Build:** Full Chrome extension build clean at `apps/extension/build/dating-aiextension-1.2.2-chrome.zip`

---

### What's DONE in this session
- Live Tinder DOM research completed → `docs/tinder-dom-research-2026-05-09.md`
- Selector constants in `index.tsx` already use real live Tinder selectors:
  - `textarea[placeholder="Type a message"]`
  - `log[aria-label="Conversation history"]`
  - `[aria-label="Card stack"]`
  - `Open Profile` button text detection
- Profile card overlay, coach banner, auto-pilot, scheduler, popup settings — all implemented
- `estimateActiveChats` already has `a[href^="/app/messages/"]` as first candidate but is NOT yet scoped to the selected tabpanel

### What's NEXT (next session priority)

#### 1. Harden `profileLogic.ts` — tabpanel-scoped chat estimation
Add `findActiveTabPanel(root, tabLabel)`:
- Looks for `[role="tab"][aria-selected="true"]` matching label text
- Resolution: aria-controls → getElementById, then positional match
Update `estimateActiveChats` priority order to:
1. `findActiveTabPanel(root, "Messages")` → `a[href^="/app/messages/"]`
2. `findActiveTabPanel(root, "Matches")` → links + `.matchListItem`
3. Global page `a[href^="/app/messages/"]`
4. Broader selectors
5. Unread badges
6. Open composer fallback

#### 2. Harden `index.tsx` — section-based profile extraction on expanded route
Add:
- `EXPANDED_PROFILE_ROUTE_PATTERNS = ["/app/recs/profile"]`
- `isExpandedProfileRoute()` → checks `window.location.pathname`
- `extractExpandedProfileSections()` → scrapes semantic section headings (About me, Essentials, Interests, Looking for, etc.)
Update `extractProfileText()` to call expanded extraction when on expanded route

#### 3. New tests in `profileLogic.test.ts`
Add `findActiveTabPanel` describe block:
- null when no tabs
- null when no selected tab
- null when selected tab label doesn't match
- finds panel via aria-controls
- finds panel via positional matching
Add tabpanel-scoped estimator test:
- DOM with selected Messages tab + aria-controls panel + 3 links → count = 3

#### 4. Run tests and build to verify
```
cd projects/dating-ai
PATH="~/.nvm/versions/node/v24.15.0/bin:$PATH" pnpm --filter @dating-ai/extension test
SKIP_ENV_VALIDATION=1 corepack pnpm --filter @dating-ai/extension build:chrome
```

#### 5. Commit and PR prep
- commit: `feat(extension): harden selectors with tabpanel-scoped estimator + expanded profile extraction`
- then open PR for review

---

### Key selectors to use (live Tinder DOM research confirmed)
| Surface | Selector | Purpose |
|---------|----------|---------|
| Recs card stack | `[aria-label="Card stack"]` | wrapper for profile cards |
| Open Profile | `button` text contains "Open Profile" | expand profile |
| Swipe actions | `button` text NOPE/LIKE/SUPER LIKE | autopilot actions |
| Sidebar tabs | `[role="tab"]` text "Matches"/"Messages" | tab detection |
| Sidebar panels | `[role="tabpanel"]` | scoped content |
| Match links | `a[href^="/app/messages/"]` | conversation threads |
| Match cards | `.matchListItem` | fallback for match cards |
| Chat composer | `textarea[placeholder="Type a message"]` | coach banner anchor, auto-type target |
| Conversation log | `log[aria-label="Conversation history"]` | chat text extraction |
| Likes Sent page | `h1` text "Likes Sent" | page detection |
| Likes Sent cards | `button[aria-label^="Open "][aria-label$="'s profile"]` | sent-like card buttons |
| Likes Sent upgrade | `button[aria-label^="Super Like "]` | super-like upgrade |
| Likes Sent container | `div[data-testid="likesYouCard"]` | card wrapper |

### Likes Sent product ideas (future slice)
1. Expiry Radar — parse "7d left" urgency text
2. Super Like Optimizer — score profiles for upgrade ROI
3. Sent-Like Triage Board — group into High/Wait/Dead
4. Opportunity Cost Meter — ignore/upgrade/move on signals
5. Second-Chance Triggering — near-expiry + strong profile → suggest upgrade
6. Outgoing Like Analytics — patterns: age, distance, conversion rate
7. Profile Enrichment for Match Prediction — parse detail pane sections

### Build commands (working)
```bash
# Full Chrome extension build
cd /home/beso/.openclaw/workspace/projects/dating-ai
SKIP_ENV_VALIDATION=1 corepack pnpm --filter @dating-ai/extension build:chrome

# Run focused extension tests
PATH="/home/beso/.nvm/versions/node/v24.15.0/bin:$PATH" /home/beso/.nvm/versions/node/v24.15.0/bin/pnpm --filter @dating-ai/extension test

# Run all monorepo tests
PATH="/home/beso/.nvm/versions/node/v24.15.0/bin:$PATH" /home/beso/.nvm/versions/node/v24.15.0/bin/pnpm test
```
