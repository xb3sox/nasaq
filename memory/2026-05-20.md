# Memory — 2026-05-20

## Session: Clinic AI OS Build

### What was built
- **Project:** Clinic AI OS — Arabic-first SaaS dashboard for clinics in Riyadh
- **Stack:** Next.js 15 App Router, TypeScript, Tailwind CSS, shadcn/ui, Supabase (PostgreSQL + RLS), OpenAI, WhatsApp Cloud API, n8n automations
- **Workspace path:** `/home/beso/.openclaw/workspace/clinic-ai-os/`

### Files created
- `supabase/migrations/001_init.sql` — Full DB schema (14 tables: clinics, profiles, branches, services, staff, customers, conversations, messages, bookings, invoices, invoice_items, reminders, ai_logs, followups, settings, webhook_events, dead_letters), RLS policies, triggers, indexes
- `supabase/migrations/002_upsert_customer.sql` — RPC `upsert_customer_by_phone()`
- `supabase/seed/seed_demo.sql` — Demo data: 1 Riyadh clinic, 2 profiles, 1 branch, 4 services, 3 doctors, 20 customers (Arabic names, Saudi phone format), 10 conversations, 30 bookings, 5 invoices
- `n8n/workflows/whatsapp-ingest-crm-upsert.json` — WhatsApp webhook → normalize → upsert customer → conversation → message → OpenAI intent → update DB
- `n8n/workflows/ai-suggest-reply.json` — Fetch messages → OpenAI → save ai_log → respond
- `n8n/workflows/create-booking-from-chat.json` — Validate → create booking → create reminders (24h + 2h) → send confirmation
- `n8n/workflows/reminder-sender.json` — Cron every 5m → query pending reminders → batch send WhatsApp → mark sent
- `nextjs/app/api/webhooks/whatsapp/route.ts` — WhatsApp webhook (mock + n8n forward)
- `nextjs/app/api/ai/suggest-reply/route.ts` — AI suggest reply (mock + n8n forward)
- `nextjs/app/api/whatsapp/send-message/route.ts` — WhatsApp send (mock + live)
- `nextjs/.env.example` — All required env vars
- `clinic-ai-os/app/page.tsx` — Arabic landing page with hero, features, pricing, FAQ, CTA
- `clinic-ai-os/app/dashboard/crm/page.tsx` — CRM list with search, filter by lead_status, actions
- `clinic-ai-os/app/dashboard/bookings/page.tsx` — Bookings list + calendar view placeholder
- `clinic-ai-os/app/dashboard/invoices/page.tsx` — Invoices with mark-paid button
- `clinic-ai-os/app/dashboard/reminders/page.tsx` — Reminder queue with status cards
- `clinic-ai-os/app/dashboard/reports/page.tsx` — Stats cards, top services, top sources, AI performance
- `clinic-ai-os/app/dashboard/settings/page.tsx` — Tabbed settings: clinic, WhatsApp, AI, team, services, reminder templates
- `clinic-ai-os/components/ui/badge.tsx` — Badge component
- `clinic-ai-os/components/ui/switch.tsx` — Switch component
- `.github/workflows/ci.yml` — CI: typecheck, lint, build, optional n8n workflow import
- `docker-compose.yml` — Postgres, n8n, app services for local dev
- `deployment/vercel.json` — Vercel config with env var mappings
- `README.md` — Setup guide

### n8n local setup
- n8n running at http://127.0.0.1:5678 (local Docker, fresh instance)
- n8n-MCP running at http://127.0.0.1:3000 (healthy)
- 4 workflows imported via CLI and published:
  - `238bad1a` — WhatsApp Ingest & CRM Upsert
  - `0a2cf98c` — AI Suggest Reply
  - `cb13e4cd` — Create Booking From Chat
  - `93c951ee` — Reminder Sender
- Smoke tests passed for all 3 webhook workflows
- Reminder sender is cron-based (no webhook)
- n8n-MCP auth token stored at `/tmp/n8n_mcp_env`

### What still needs credentials to complete
1. **Supabase URL + SUPABASE_SERVICE_ROLE_KEY** → DB writes end-to-end verification, RLS test
2. **OPENAI_API_KEY** → Real Arabic AI replies (currently mock)
3. **WhatsApp credentials** (WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID) → Live message sending
4. **Vercel token** → Production deployment of Next.js

### Status
- MVP fully scaffolded and running in mock mode locally
- Next.js build passes with zero TS errors
- All core pages built: landing, dashboard, CRM, bookings, inbox, reminders, reports, invoices, settings
- Blockers: need real Supabase + OpenAI keys to go beyond mock mode

### Decisions made
- Git identity: `basem <basem.igi@gmail.com>`
- MOCK_MODE=true by default for local dev
- Separate n8n workflow per feature (not grouped)
- Triggers: webhooks for realtime, cron every 5m for reminders
- Arabic-first, RTL layout
- shadcn/ui component library
- Supabase REST API for n8n HTTP nodes (service role key)
- n8n-MCP used for local workflow management and audit

### Pricing (from spec)
- Basic: 7,000 SAR setup + 1,500 SAR/mo
- Pro: 12,000 SAR setup + 3,000 SAR/mo
- Premium: 20,000 SAR setup + 5,000 SAR/mo

## Session: OpenClaw Setup Audit

### Checks run
- `openclaw skills check`: 67 eligible/visible skills, 1 missing requirement (`gemini` CLI bin)
- `openclaw doctor`: gateway running, browser profile configured, one stale `openai-codex/*` session route warning
- `openclaw gateway status --deep`: gateway bound to loopback `127.0.0.1:18789`, token auth, one TUI client, admin-capable
- `openclaw security audit --deep`: 2 critical skill-code pattern findings, 4 warnings, 2 info
- Linux host read-only checks: Ubuntu 24.04.4 LTS, no active SSH service, 832G free disk, 11G available RAM

### Findings
- Good: gateway is loopback-only locally, token auth enabled, no open Telegram groups, no SSH daemon active.
- Watch: Tailscale Serve exposes gateway to the tailnet; keep tailnet trusted.
- Fix later with approval: set `commands.ownerAllowFrom`, run `openclaw doctor --fix`, review or disable flagged skills, pin `@openclaw/codex`, trim weak fallback models.
- Internal cleanup done: updated `HEARTBEAT.md` with low-noise proactive checks and `TOOLS.md` with OpenClaw CLI/gateway notes.
