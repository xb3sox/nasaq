# 2026-05-21

- Browser config decision: Basem asked to use OpenClaw default/docs settings and not custom browser hacks. Reverted browser setup to docs-standard:
  - `~/.openclaw/openclaw.json` browser user profile: `driver=existing-session`, `attachOnly=true`, `userDataDir=~/.config/google-chrome`, color `#00AA00`.
  - `~/.agent-browser/config.json`: `{"profile":"~/.config/google-chrome/Default"}`.
  - Removed custom `/tmp/chrome-cdp-session` flow and `~/.local/bin/launch-chrome-agent`.
  - TOOLS.md was updated earlier to document the clean browser setup, but during this flush it is read-only.
- Open task from Telegram before compaction: Basem asked in caveman mode to open the browser so he can log in, then asked to close and open again to check if sessions are preserved. Prior attempt failed/aborted before a useful reply. Resume by opening Chrome/OpenClaw browser using the default existing-session setup, then close/reopen and verify the login/session persists.
- Correction after audit: real Chrome profile setup above is obsolete. Current intended setup:
  - OpenClaw browser: enabled, `defaultProfile=user`, `headless=true` in this runtime.
  - Managed profile path: `/home/beso/.openclaw/browser-profiles/user`.
  - `~/.agent-browser/config.json`: points `profile` to managed user profile.
  - Rule: do not use `~/.config/google-chrome/Default` unless Basem explicitly approves. Close agent-browser session before changing profile, otherwise daemon ignores `--profile`.
- Final browser decision from Basem: remove `agent-browser` from default workflow and use OpenClaw native browser defaults from current docs.
  - `~/.openclaw/openclaw.json`: `browser.enabled=true`, `browser.defaultProfile="openclaw"`, no `browser.headless` override, no custom `browser.profiles`.
  - `~/.agent-browser/config.json` archived under `~/.agent-browser/disabled/`.
  - Default browser = isolated OpenClaw-managed `openclaw` profile. `user` profile attaches to real signed-in Chrome only when Basem explicitly asks.
- Runtime correction: set `browser.headless=true` because Gateway host has no `$DISPLAY`/`WAYLAND_DISPLAY`. OpenClaw browser doctor passes for profile `openclaw` after starting with `openclaw browser --browser-profile openclaw start --headless`.
- `agent-browser` binary and package were disabled by moving them into `~/.agent-browser/disabled/agent-browser-YYYYMMDD-HHMMSS/` rather than deleting permanently.
- Basem asked to reset browser to default. Current config: `browser.enabled=true`, `browser.defaultProfile="openclaw"`, no `browser.headless`, no `browser.profiles`. Browser processes stopped; old profile directories left untouched.
- Basem asked to remove old browser profiles and keep only real Chrome plus current OpenClaw default. Moved old dirs to Trash/archive:
  - `/home/beso/.openclaw/browser-profiles/openclaw` (about 4.3G)
  - `/home/beso/.openclaw/browser-profiles/user` (about 66M)
  - Archive path: `/home/beso/.local/share/Trash/files/openclaw-old-browser-profiles-20260521-093155/`
  - Kept: `/home/beso/.config/google-chrome/Default` and `/home/beso/.openclaw/browser/openclaw/user-data`.
- Basem then asked to restore old `openclaw` profile and remove current OpenClaw default. Restored:
  - `/home/beso/.local/share/Trash/files/openclaw-old-browser-profiles-20260521-093155/openclaw` -> `/home/beso/.openclaw/browser/openclaw/user-data`
  - Previous current default moved to `/home/beso/.local/share/Trash/files/openclaw-current-browser-user-data-20260521-094752/`
  - Restored profile size about 4.3G, mostly Chrome `OptGuideOnDeviceModel` files. Browser doctor passed after `openclaw browser --browser-profile openclaw start`; browser stopped afterward.
- Basem also asked to restore old `.openclaw/browser-profiles/user`. Restored:
  - `/home/beso/.local/share/Trash/files/openclaw-old-browser-profiles-20260521-093155/user` -> `/home/beso/.openclaw/browser-profiles/user`
  - Size about 66M, Chrome profile name `Your Chromium`.
- Latest pre-compaction browser state after Basem asked "reset to default" and then "?":
  - Browser config is intended to be OpenClaw native default only.
  - `~/.openclaw/openclaw.json` should keep browser enabled with `defaultProfile="openclaw"` and no custom profile overrides.
  - Current default OpenClaw browser data lives under `/home/beso/.openclaw/browser/openclaw/user-data`.
  - Old custom/managed profile dirs were archived, not deleted permanently, under Trash path above.
  - `agent-browser` should remain disabled/archived and not used by default.
  - If browser verification is needed after compaction: use OpenClaw native browser tooling/default profile, not `agent-browser`.

- 10:04: Removed empty legacy OpenClaw browser profile root `~/.openclaw/browser-profiles` by moving it to trash: `~/.local/share/Trash/files/openclaw-legacy-browser-profiles-empty-20260521-100451`. Active profile remains `~/.openclaw/browser/openclaw/user-data`.

- 10:05: Permanently removed old OpenClaw browser profile trash entries: `openclaw-old-browser-profiles-20260521-093155`, `openclaw-current-browser-user-data-20260521-094752`, and `openclaw-legacy-browser-profiles-empty-20260521-100451`. Active profile remains `~/.openclaw/browser/openclaw/user-data`.

- 10:18: Reset OpenClaw-managed browser profile by moving `~/.openclaw/browser/openclaw/user-data` (4.3G) to Trash at `~/.local/share/Trash/files/openclaw-browser-user-data-reset-20260521-101830` and recreating a fresh empty `user-data` directory. Main size cause was Chrome `OptGuideOnDeviceModel` (~4.0G). Config remains `browser.enabled=true`, `browser.defaultProfile="openclaw"`. Real Chrome `~/.config/google-chrome/Default` untouched.

- Context/browser cleanup fragments were consolidated. Durable state:
  - OpenClaw native browser profile is the default; avoid `agent-browser` unless explicitly needed.
  - Active OpenClaw profile is `openclaw`; real Chrome profile stays untouched unless Basem asks.
  - Agent/browser setup details live in `TOOLS.md`, not scattered memory fragments.
