# Memory — 2026-05-22

## Clinic AI OS MVP

### What was built
- Full Next.js 16 Arabic RTL dashboard at `/home/beso/.openclaw/workspace/clinic-ai-os/`
- WhatsApp send adapter (mock + Meta Cloud API), AI provider (deterministic + OpenAI + Gemini)
- Supabase schema (all tables + RLS + seed), 32 tests passing, build clean
- Landing page + 7 dashboard pages: overview, inbox, bookings, crm, reminders, invoices, reports, settings
- Landing page with Arabic hero, features, pricing (3 tiers), FAQ, CTA
- Deployed on Vercel: https://clinic-ai-os.vercel.app
- GitHub: https://github.com/xb3sox/clinic-ai-os (repo has Jules workflow for AI code review)
- GitHub username: xb3sox

### WhatsApp account creation attempt
- Used agent-browser automation
- Got to step 2 of Gmail signup twice (saw "besoa0669@gmail.com" suggested)
- Blocked by Google bot detection on fresh IP before completing
- Not critical — MVP works in mock mode without real credentials

### To deploy Beso's own Vercel account
1. Run `vercel --prod` in `/home/beso/.openclaw/workspace/clinic-ai-os` (already authenticated with Basem's account)
2. Add env vars to Vercel dashboard
3. For CI/CD: add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID to GitHub repo secrets
4. Deploy workflow: .github/workflows/deploy.yml

### Key files
- SPEC.md — full product spec
- README.md — setup guide  
- .env.example — local dev env vars
- .env.production.example — production env template
- supabase/migrations/002_full_schema.sql — DB schema
- vercel.json — deployment config
- .github/workflows/jules.yml — AI code review workflow
- .github/workflows/deploy.yml — CI/CD deploy workflow

### Gmail account status
- Attempted: besoai.system@gmail.com → besoa0669@gmail.com suggested
- Got past step 1, blocked by Google on step 2 (phone verification)
- No phone verification available in this session
- Not blocking — deploy works regardless

### Live URLs
- App: https://clinic-ai-os.vercel.app (original alias)
- App (new): https://clinic-ai-os-theta.vercel.app (latest production deploy)
- Dashboard: https://clinic-ai-os.vercel.app/dashboard
- Repo: https://github.com/xb3sox/clinic-ai-os
- Deploy: `vercel --prod --yes` works (authenticated as xb3sox)
- Vercel CI/CD: .github/workflows/deploy.yml added (needs secrets in GitHub)
- Jules workflow: .github/workflows/jules.yml updated to also trigger on PRs

## Reviewer workflow memory
- Basem wants Jules + Copilot review flow remembered: use Google Jules for GitHub PR creation/async implementation, then run GitHub Copilot CLI `/review` on Jules-created PRs before merge.
- Correct Jules CLI package: `@google/jules`; avoid unrelated `jules` npm package. Authenticate with `jules login`.
- Copilot CLI launches through `gh copilot`; `/review` is documented by GitHub as terminal code review and may require interactive command approvals.
- Updated workflow preference after doc check: SDD is the master policy; Sabi/Hermes Kanban is default for durable build/fix/ship work; Jules is an async GitHub PR lane; Cursor CLI is local read-only review/scoped-fix lane; Copilot CLI `/review` is final independent PR gate.
- Local/internal AI prompts should start with caveman mode to reduce token cost: `Caveman mode: terse, no fluff, direct output, no long explanation.` Use normal prose for user-facing specs, docs, PR bodies, and commit messages.
- Hermes Kanban docs checked: durable SQLite board at `~/.hermes/kanban.db`, task dependencies via parents, `review-required:` blocks for code review, and CLI/tool surfaces. Current profile list shows only `default`; gateway running; board empty.
- UI/UX strategy refined: do not beautify blindly. Audit screenshots first, then design-system/tokens/components/app-shell, then page-level responsive fixes, then final polish. Required skill stack: `impeccable`, `ui-ux-pro-max`, `web-design-guidelines`, `polish`, `agent-browser`; verify 390/430/768/1024/1440 px, RTL, WCAG AA, no horizontal scroll, no clipped/overlapped text, and 44-48px touch targets.
- Created Hermes Kanban board `clinic-ai-os` for UI/UX rebuild. Cards: `t_41537c33` audit (running), `t_a075f03c` design system, `t_ad184b08` dashboard pages, `t_2a810a27` landing, `t_95d7bc35` verification. Downstream cards were blocked after creation to prevent parallel file conflicts; unblock sequentially after parent output is reviewed.

## Pre-compaction flush — 10:26 Asia/Riyadh
- Basem asked to keep workspace context clean and non-duplicative: SDD should remain the master source of truth for build workflow, while `TOOLS.md` only records Basem-specific/local tool state and preferences.
- For product UI quality, Basem wants world-class UX/UI and responsive behavior treated as a hard gate, especially for Clinic AI OS. Default workflow: audit first, use design-system/tokens first, then page polish, then screenshot/browser verification across mobile/tablet/desktop and RTL.
- Basem explicitly wants cost-aware local AI usage: when prompting Cursor CLI, Copilot CLI, Codex, Jules, or Sabi for internal work, start with caveman mode to reduce token usage; do not force this onto polished user-facing output.
- Cross-agent build/review policy to preserve: use Sabi/Hermes Kanban heavily for durable build/fix/ship work; use Jules for async GitHub PR implementation; use Cursor CLI for local codebase review/scoped fixes; use GitHub Copilot CLI `/review` as final PR review gate before merge.
- During the last workflow cleanup, reference files like `MEMORY.md`, `TOOLS.md`, and skills were edited earlier in the session, but this flush must only append to `memory/2026-05-22.md`.

## Workspace setup cleanup — 10:35 Asia/Riyadh
- Removed duplicate daily-note block at top of `memory/2026-05-22.md`.
- Trimmed `TOOLS.md` so it stores local tool state instead of repeating full workflow policy.
- Updated `MEMORY.md` key decisions to point to source-of-truth files: `sdd` for policy, `requesting-code-review` for commands, `TOOLS.md` for local auth/install state.
- Local tool state checked: `gh` authenticated as `xb3sox`; `gh copilot` available through GitHub CLI; `vercel` installed/authenticated as `xb3sox`; `jules` and `cursor-agent` not installed locally yet.
- Collaboration rule preserved: one writer lane per file set. For Clinic AI OS UI work, Sabi/Hermes Kanban remains active builder; Jules/Cursor/Copilot should be review/PR gates until ownership changes.
- Fixed Hermes Kanban blocker: original UI audit card `t_41537c33` crash-looped because it forced OpenClaw skill names unavailable to Hermes. Reclaimed and blocked it, created clean replacement `t_3662975c` with no forced skills, relinked `t_3662975c -> t_a075f03c`, and dispatched it.

## Workspace context audit — 10:57 Asia/Riyadh
- Basem clarified: audit workspace context only, not the Clinic AI OS project.
- Fixed context issues: clarified Sabi delegation scope in `AGENTS.md`, made `USER.md` explicitly private, changed heartbeat memory distill from auto-commit to report-only unless Basem asks, and compressed `MEMORY.md` to reduce noisy product-specific details and avoid storing exact signup/secret placeholders in long-term memory.
- Leave `clinic-ai-os/` code and project files untouched during this audit.

## 15:30 — Bitwarden Secrets Manager configured
- `bws` access token injected via env block in openclaw.json.
- `bw` logged in; passwords imported from 1Password.
- 1Password abandoned; can cancel subscription
- Bitwarden free tier for both Password Manager + Secrets Manager

## 16:30 — Context file cleanup

- Root context reduced toward one-source-per-topic: `COMMAND_CENTER.md` now owns active priorities, product list, simple CRM, and weekly review.
- `BUSINESS_OS.md`, `PRODUCTS.md`, `CRM.md`, and `WEEKLY_REVIEW.md` were merged into `COMMAND_CENTER.md` and moved to trash.
- `IDENTITY.md` kept as compact OpenClaw identity metadata; detailed voice stays in `SOUL.md`.
- `FINANCE.md` stays separate and private for money details.
- `AGENTS.md` and `MEMORY.md` updated to point to the simplified context structure.
- Same-day timestamp memory fragments were consolidated into this canonical daily note and moved to trash.
- Fixed stale context: Clinic AI OS path now uses the real workspace path and n8n count is 5 workflows / 4 active.
- Trimmed `SOUL.md` and `USER.md` duplicates: Sabi workflow stays in `AGENTS.md`; business expenses stay in `FINANCE.md`.
- Added `FINANCE.md` to `.gitignore` so private money ledger stays local.
